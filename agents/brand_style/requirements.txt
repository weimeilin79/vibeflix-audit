# Agent Runtime deploy manifest (adk deploy agent_engine installs THIS file from
# inside the agent folder — the shared ../requirements.txt is not shipped).
# Keep in sync with agents/requirements.txt.
fastapi>=0.110.0
uvicorn>=0.28.0
pydantic>=2.6.0
python-dotenv>=1.0.0
mcp>=1.0,<2
google-adk[a2a]==2.3.0
google-genai>=1.0.0
aiohttp>=3.9.0
google-cloud-firestore>=2.16.0
google-cloud-pubsub>=2.21.0
# vibeflix-common is not on PyPI — installed from the repo subdirectory.
# Private repo? Swap for an Artifact Registry python repo or a token URL.
vibeflix-common @ git+https://github.com/weimeilin79/vibeflix-audit.git#subdirectory=packages/vibeflix-common
