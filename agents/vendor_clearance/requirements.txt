# Agent Runtime deploy manifest (adk deploy agent_engine installs THIS file from
# inside the agent folder — the shared ../requirements.txt is not shipped).
# Keep in sync with agents/requirements.txt.
fastapi>=0.110.0
uvicorn>=0.28.0
pydantic>=2.6.0
python-dotenv>=1.0.0
mcp>=1.0,<2
google-adk[a2a,agent-identity]==2.3.0
google-genai>=1.0.0
aiohttp>=3.9.0
google-cloud-firestore>=2.16.0
google-cloud-pubsub>=2.21.0
# vibeflix-common is not on PyPI and the repo is private — the deploy vendors
# the package INTO this folder (deploy_agents.sh / instruction-dev.md step 3b),
# and it installs from the image's absolute path:
/app/agents/vendor_clearance/_vendor/vibeflix-common
